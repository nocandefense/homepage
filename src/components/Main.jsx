function Main({ children }) {
    return (
        <section className="bg-gray-700 backdrop-filter backdrop-opacity-10 p-5 shadow-sm rounded-md">
            {children}
        </section>
    );
}

export default Main;
